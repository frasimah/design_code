# Design Code Panel

> Интеллектуальный каталог мебели и материалов с AI-ассистентом

## 🎯 О проекте

Design Code Panel — это веб-приложение для управления каталогом мебели и отделочных материалов с интегрированным AI-консультантом на базе Google Gemini. Система позволяет:

- 🔍 Поиск товаров по текстовому описанию или изображению
- 💬 Консультации с AI-ассистентом по подбору материалов
- 📁 Создание проектов и сохранение избранных товаров
- 📄 Генерация коммерческих предложений в PDF
- 🛒 Интеграция с WooCommerce

## 🏗️ Архитектура

```
┌─────────────────┐     ┌─────────────────┐
│   Next.js 16    │────▶│   FastAPI       │
│   Frontend      │     │   Backend       │
│   (React 19)    │     │   (Python 3.10) │
└─────────────────┘     └────────┬────────┘
                                 │
         ┌───────────────────────┼───────────────────────┐
         │                       │                       │
    ┌────▼────┐           ┌──────▼──────┐         ┌──────▼──────┐
    │ ChromaDB│           │ Google      │         │ WooCommerce │
    │ (RAG)   │           │ Gemini AI   │         │ API         │
    └─────────┘           └─────────────┘         └─────────────┘
```

## 📁 Структура проекта

```
design_code/
├── src/                    # Python бэкенд
│   ├── api/                # FastAPI роуты
│   ├── rag/                # RAG система (ChromaDB)
│   ├── consultant/         # AI-консультант
│   └── templates/          # HTML шаблоны (PDF)
├── furniture-catalog/      # Next.js фронтенд
│   ├── app/                # App Router
│   ├── components/         # React компоненты
│   └── lib/                # Утилиты и API клиент
├── config/                 # Конфигурация
├── data/                   # Данные (не в Git)
├── requirements.txt        # Python зависимости
└── INSTALL.md              # Инструкция по установке
```

## 🚀 Быстрый старт

```bash
# Клонирование
git clone https://github.com/frasimah/design_code.git
cd design_code

# Бэкенд
python3 -m venv venv && source venv/bin/activate
pip install -r requirements.txt

# Фронтенд
cd furniture-catalog && npm install && cd ..

# Конфигурация
cp .env.example .env
# Добавьте GEMINI_API_KEY в .env

# Запуск через PM2 (Рекомендуется)
pm2 start ecosystem.config.js

# Или ручной запуск (два терминала)
# Терминал 1:
uvicorn src.api.server:app --port 8001
# Терминал 2:
cd furniture-catalog && npm run dev
```

Подробная инструкция: [INSTALL.md](./INSTALL.md)
Доступно по адресу: **panel.de-co-de.ru** (в продакшне) или **localhost:3001** (локально)

## ⚙️ Технологии

### Бэкенд
- **FastAPI** — REST API
- **ChromaDB** — векторная база данных для RAG
- **Google Gemini** — LLM для AI-консультанта
- **Jinja2** — HTML шаблоны для PDF

### Фронтенд
- **Next.js 16** — React фреймворк
- **React 19** — UI библиотека
- **Tailwind CSS 4** — стилизация
- **Radix UI** — компоненты

## 📋 Конфигурация AI

> [!IMPORTANT]
> **Модель: `gemini-3-flash-preview`**
> 
> Эта модель обязательна для корректной работы парсинга PDF и AI-консультанта.

## 📝 Лицензия

Proprietary © 2026

---

*Разработано с ❤️ для Design Code*
